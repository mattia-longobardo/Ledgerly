# SDD ledger — plan: docs/superpowers/plans/2026-09-04-phase-2-integrations.md
Spec: docs/superpowers/specs/2026-09-02-finance-company-platform-design.md (read; binding authority)

This is the running ledger kept during execution (originally
`.superpowers/sdd/2026-09-04-phase-2-integrations/progress.md`, copied here
verbatim by Task 20 for discoverability alongside the checkpoint, per the
Phase 0/1 precedent). The pre-flight conflict scan referenced below as
"preflight-scan.md" is the full 333-line table at
`.superpowers/sdd/2026-09-04-phase-2-integrations/preflight-scan.md`; every
per-task brief/report/review diff is in that same directory as
`task-N-brief.md` / `task-N-report.md` / `task-N-review.diff`.

Baseline before Task 1: typecheck clean, 609 unit / 55 files, 33 itest / 12 files.
Ruling P2-C1: implementation proceeds on `main` with no worktree — the project hook blocks branch/worktree creation and Phase 0/1 ruling R1 set this precedent on this checkout — cost if wrong: commits land on main directly; each task commits separately and can be reverted individually.

## Pre-flight scan
(pending — scanner dispatched)

## Progress

### Pre-flight rulings (2026-09-04)
Scan: preflight-scan.md — 52 pair rows, 20 task rows, 20 rubric findings, 5 spec gaps, 8 hard cross-task conflicts.

Ruling P2-C2: repair the plan before executing rather than executing and absorbing the defects in fix rounds — 8 hard conflicts make several tasks' own verification steps unsatisfiable (T11 step 9 typecheck, T18's own test), so execution would fail deterministically — cost if wrong: one extra planning pass before Task 1.
Ruling P2-C3 (spec §3.4, webhook inline work): the webhook route verifies, records the delivery, and enqueues a `sync_runs` row with status `queued` and trigger `webhook`; the existing job tick drains queued runs. Spec §3.4 says webhook endpoints "enqueue a job row rather than doing work inline" — cost if wrong: webhook-triggered syncs are delayed to the next tick instead of being immediate.
Ruling P2-C4 (`sync_jobs` has no reader): keep the table and give it one — the sync engine looks up the `sync_jobs` row for (connection, kind), refuses a disabled kind, and stamps `sync_runs.job_id`. Spec §5.2 names the table and §5.10 indexes runs by job — cost if wrong: an unused column if a later phase moves scheduling elsewhere.
Ruling P2-C5 (spec §6 `SyncContext` cursor state): `sync_jobs.cursor jsonb` holds per-kind cursor state; `SyncContext` exposes `cursor` and `setCursor(next)`, persisted when the run succeeds — cost if wrong: Phase 3 incremental transaction sync has to add it.
Ruling P2-C6 (`ProviderCode` drops `payroll_silo`): the union carries only `wallet` and `trek` this phase; Phase 4 adds `payroll_silo` with the document store it needs. The spec lists three provider codes — cost if wrong: a one-line union widening in Phase 4.
Ruling P2-C7 (`provider_links` unique key gains `user_id`): deliberate, it is deferred minor (b) from the Phase 0/1 ledger and must land before a second user exists. Spec §5.2 states the key without `user_id` — cost if wrong: two users could not hold the same external id, which is the bug being fixed.
Ruling P2-C8 (framework naming drift): `toJobs`→`toSyncRequests` and `verify(req)`→`verify(req, secret)` are adopted; the spec's §6 sketch is illustrative TypeScript, and a verifier needs the secret it checks against — cost if wrong: rename.
Ruling P2-C9 (spec §3.2 versioned mutations): connection mutations that change settings or disconnect send an expected `version` and throw `ConnectionVersionMismatchError` on conflict; the plan currently swallows it — cost if wrong: none, this is strictly stricter.
Ruling P2-C10 (env validation in tests): `env()` gains a `resetEnvCache()` test seam and the integration vitest config gets a setup file providing the full required env, rather than each itest setting one variable — cost if wrong: a setup file to delete later.
Ruling P2-C11: every plan-mandated rubric defect the scan listed (swallowed errors, verbatim duplication across adapters and test files, `db: {} as never` casts, assertion-free tests, prose-only code steps, knowingly-red OpenAPI drift commits) is repaired in the plan rather than shipped and flagged by reviewers — cost if wrong: a longer plan.
Ruling P2-C12: the repaired plan's two documented deferrals are accepted — the public webhook endpoint is not rate limited, and `rejected` webhook deliveries are not surfaced to users. Both carry to Phase 9 (hardening); the endpoint still verifies HMAC before doing any work — cost if wrong: an unauthenticated caller can force HMAC checks until Phase 9 adds a limiter.
Plan repaired 2026-09-04: 5730 -> 9349 lines, still 20 tasks; rulings P2-C13 (pool-bound deps, self-opened transactions) and P2-C14 (route/action deletion moves to Task 11) added by the repair.
Ruling P2-C15: implementers do NOT run `graphify update .` and do NOT commit `graphify-out/`; the graph is regenerated once in a single commit at the end of the phase. `graphify update` rewrites a 92k-line graph.json, which made Task 1's review package 9.5 MB and would have swamped every reviewer's context. This overrides the plan's Global Constraints line "Run `graphify update .` after each task that changes code" — cost if wrong: the graph is stale mid-phase, which only affects agents' own navigation, and is fixed by the end-of-phase commit.
Task 1: complete pending review (commit 9be34cb) — graphify-out churn included in that commit; excluded from the review package.
Ruling P2-C16: tasks are pipelined — the next task's implementer is dispatched while the previous task's reviewer runs. Only one implementer is ever live, so there are no write conflicts; reviewers are read-only. Fix commits always land at HEAD, so a fix round's diff base is the HEAD recorded when the fix is dispatched and stays clean. Not pipelined when the next task directly builds on the code under review — cost if wrong: a task builds one commit on top of code that later needs a fix, which the fix round then has to carry.
Task 2: dispatched (base 9be34cb)
Task 1: review 1 — spec compliant; 2 Important, 1 Minor. (I1) the itest runs under role "user" where RLS enforces, but every production caller runs upsertSeen under role "system" which bypasses RLS, so the silent cross-user overwrite the task exists to fix is never demonstrated; (I2) MemoryProviderLinksRepository.upsertSeen still matches on (provider, entityType, externalId) without userId, reproducing the original bug in the test double; (m3) stale comment in wallet-accounts-sync.ts:12-17 now misdescribes the schema.
Task 1: fix round 1 held until Task 2's implementer reports — both touch the accounts module (memory-repositories.ts), and only one implementer may write at a time.
Task 2: implemented (commit 8c45894), under review. Implementer concern: had to also change wallet-accounts-sync.test.ts (outside the brief) because the existing httpRequest mock never intercepted requestJson's same-file call, so the test was making a real network call; added a walletAccountsSource mock. Flagged to the reviewer to judge.
Task 1: fix round 1 dispatched to the original implementer (3 findings: system-role regression test, memory repo double, stale comment).
Task 2: complete (commits 9be34cb..8c45894, review clean — 0 Critical, 0 Important, 2 Minor).
Task 2: Ruling P2-C17 (resolves the reviewer's ⚠️): the fetch is out of the accounts/links transaction at all three call sites, which is what the task required, but in `wallet-accounts-sync.ts` `syncOwner` still runs inside `withJobLock`'s own `db.transaction` + advisory lock, so the brief-mandated doc comment on sync-provider-accounts.ts:76-80 overstates the win (the real gain is one held pool connection instead of two, not a released advisory lock). Correcting the comment is deferred to Task 11, which rewrites this job onto the sync engine and owns that file — cost if wrong: a comment stays imprecise for ten tasks. Moving the fetch outside `withJobLock` was rejected: it would fetch even when another instance holds the lock.
Task 2: minor (deferred): the doc comment above; wallet-accounts-sync.test.ts fully mocks syncProviderAccounts so no test in that file exercises the real fetch path (pre-existing, confirmed not a regression).
Task 1: fix round 1 dispatched -> commit 0dee2f5; scoped re-review dispatched (base 8c45894).
Task 3: dispatched (base 0dee2f5).
Task 1: fix round 1/5 (3 addressed, 0 open — system-role itest, memory repo userId guard, corrected comment; commits 8c45894..0dee2f5)
Task 1: complete (commits 65aec40..0dee2f5, review clean)
Task 3: implemented (commit 9680187, DONE_WITH_CONCERNS — record.itest.ts needed withSystemContext once audit_events got FORCE RLS), under review.
Task 4: dispatched (base 9680187) — credential cipher + resetEnvCache seam + integration-test env setup.
Task 3: complete (commits 0dee2f5..9680187, review clean — 0 Critical, 0 Important, 2 Minor).
Task 3: minor (deferred): record.itest.ts comment implies a null-actor recordAudit call shape that no production caller uses; task-3-report.md header says DONE while the implementer reported DONE_WITH_CONCERNS.
Task 4: implemented (commit 4128214). Out-of-brief: APP_ENCRYPTION_KEY added to 10 pre-existing unit test env fixtures. Reviewed on opus (security-critical crypto).
Task 5: dispatched (base 4128214) — integration tables, RLS, provider seed, migration 0010.
Task 4: review 1 — spec compliant on every Produces name/signature; crypto verified correct (per-call random IV, tag verified via final() failing closed, key id bound as AAD and re-derived from the CLAIMED id so a relabelled blob fails, first-Map-entry active key, no secret in any error/log path, no secret comparison in JS). 2 Important, 6 Minor.
Task 4: Ruling P2-C18 (Important 2, plan-mandated deployment gap): accepted as deliberate. `APP_ENCRYPTION_KEY` became required at Task 4 but docker-compose.yml and .env.example only gain it in Task 19, so commits 4128214..Task-18 cannot boot the deployed stack. Phase 2 deploys as a unit after Task 20, never from an intermediate commit; Task 19's compose change is a release gate. Production is unaffected — it runs the Phase 1 image and main is not pushed — cost if wrong: someone deploying a mid-phase commit gets "Invalid environment configuration" for the whole app, not just integrations.
Task 4: Ruling P2-C19: one Minor is promoted into the fix round rather than deferred — no test proves `resetCredentialCipher()` or `resetEnvCache()` actually clear their caches, and seven later tasks' integration tests depend on those two seams. A seam that silently does nothing would fail those tasks in a way that looks like their own bug — cost if wrong: two extra tests.
Task 4: minor (deferred): lenient base64 (a garbled key can decode to a valid-looking 32 bytes — suggest round-trip check); version byte written but not authenticated (downgrade seam once a v2 exists); `parsed as Record<string,string>` unchecked cast (nested object yields non-string values typed as strings); empty key-list entries silently dropped; TEST_ENCRYPTION_KEY fixture literal duplicated across 10 test files; no test for the malformed-blob or non-object branches.
Task 4: fix round 1 held until Task 5's implementer reports (one writer at a time).
Task 5: implemented (commit 0542911, DONE, no concerns), under review.
Task 4: fix round 1 dispatched (base 0542911 at dispatch time; 2 findings).
Task 5: complete (commits 4128214..0542911, review clean — 0 Critical, 0 Important, 2 Minor).
Task 5: minor (deferred): no runtime RLS test for sync_jobs or webhook_deliveries (reachability rests on migration inspection; mirrors account_balances precedent); the new bytea customType is never round-tripped with a real Buffer (first use of that custom type in the codebase).
Task 4: fix round 1 dispatched -> commit 691706d (JSON.parse guarded; both cache-reset seams tested, implementer reports each verified to fail with the seam body removed). Scoped re-review dispatched (base 0542911).
Task 6: dispatched (base 691706d) — IntegrationProvider contract, SyncContext, registry, shared HMAC verification.
Task 4: fix round 1/5 (2 addressed, 0 open — guarded JSON.parse with a static message, both seam tests proven to be real regression tests; commits 0542911..691706d)
Task 4: complete (commits 9680187..691706d, review clean)
Task 6: implemented (commit 7661740), under review. Note: `npm run lint` fails in this environment for a pre-existing unrelated reason (next lint project-dir arg); lint is not among the phase exit checks (typecheck, test, test:integration, build, e2e).
Task 7: dispatched (base 7661740) — connection domain, ports, in-memory repositories.
Task 6: complete (commits 691706d..7661740, review clean — 0 Critical, 0 Important, 2 Minor).
Task 6: minor (deferred): hmacSignatureVerifier declares `headerName: string = DEFAULT` rather than the brief's `headerName?: string` (equivalent, but deviates from verbatim); task-6-report self-review prose is self-contradictory about when the HMAC is computed (report only, not code).
Task 7: implemented (commit b4f9579), under review.
Task 8: dispatched (base b4f9579) — Drizzle repositories + pool-bound integrationDeps (ruling P2-C13).
Task 7: complete (commits 7661740..b4f9579, review clean apart from one plan-mandated Important, ruled below).
Task 7: Ruling P2-C20: the Important finding — deps.ts's JSDoc names "a Wallet page fetch, a Trek MCP conversation", breaching the unconditional constraint "provider names appear only in *-adapter.ts" — is real but prose-only: no identifier, field or type is provider-specific, so the constraint's purpose (a third provider can be added without touching the framework) is not harmed. It does not enter a fix round on its own; it joins the deferred-minor cleanup wave batched before Task 19 — cost if wrong: a doc comment names two products for twelve tasks.
Task 7: minor (deferred): reword deps.ts JSDoc to drop product names.

### Deferred-minor cleanup wave (batch before Task 19)
- T2: sync-provider-accounts.ts:76-80 doc comment overstates the win (advisory lock still held) — but Task 11 rewrites this file, so fix there instead.
- T3: record.itest.ts comment implies a null-actor call shape no caller uses.
- T4: lenient base64 decode; version byte not in AAD; `as Record<string,string>` cast; empty key entries dropped; TEST_ENCRYPTION_KEY literal duplicated in 10 files.
- T6: hmacSignatureVerifier `headerName: string = D` vs brief's `headerName?: string`.
- T7: deps.ts JSDoc names Wallet and Trek.
Task 8: implemented (commit 2c0dcd3), under review. Implementer changed a fixture in the brief's own itest (connection created "disconnected" then asserted as a webhook candidate, which by contract returns only "connected") — fixture changed, not the repository.
Task 8: Ruling P2-C21: the divergence the implementer flagged — Task 7's MemoryConnectionsRepository.candidatesForWebhook does not filter by status while the Drizzle implementation does — is load-bearing and must be fixed, not deferred. Task 15's webhook use case is unit-tested against the memory repository, so the divergence would let a disconnected connection look like a valid webhook target in every unit test while the database rejects it in production. This is the same memory-vs-database divergence class that Task 1 already hit. It is folded into Task 8's fix round because that implementer has both implementations in context — cost if wrong: a one-line predicate change lands in the wrong task's commit.
Task 8: review 1 — spec compliant (pool-bound deps verified structurally, ciphertext round-trips as bytes, version conflict surfaced not swallowed); 1 Important + 1 promoted Minor. Fix round 1 dispatched.
Task 8: Ruling P2-C22: the list() ordering divergence (Drizzle sorts by provider, memory fake returns insertion order) is promoted from Minor into the fix round — this phase has hit the memory-vs-database divergence class three times now (T1 provider_links, T8 candidatesForWebhook, T8 list), and the accounts module already treats identical ordering as the standard — cost if wrong: one extra line and one test.
Task 8: minor (deferred): this task's own itest exercises one user only; per-user isolation for the new query shapes rests on Task 5's integrations-rls.itest.ts.
Carry-forward into Tasks 9, 10, 15 dispatches: inUserContext/inSystemContext both open on the pool, so they must be called SEQUENTIALLY (system context returns, then user context opens) and never nested inside one another, or one logical operation holds two pool connections.
Task 8: fix round 1/5 (2 addressed, 0 open — memory fake now filters status and sorts by provider, both proven real regression tests; commits 2c0dcd3..30e6f94)
Task 8: complete (commits b4f9579..30e6f94, review clean)
Task 9: implemented (commit 5071218), under review.
Task 10: dispatched (base 5071218) on opus — the sync engine; five later tasks depend on it.
Task 9: complete (commits 30e6f94..5071218, review clean — 0 Critical, 0 Important, 2 Minor).
Task 9: resolves the reviewer's ⚠️ — whether the disconnect policy actually governs the data left behind lives in each adapter's onDisconnect, i.e. Tasks 11 and 12. Carried into those two reviews: verify wallet's onDisconnect honours keep/archive/purge, and that trek's never deletes leave data under any policy (plan ruling P2-11). Task 9's own responsibility (pass the effective policy through, purge the credential unconditionally, one transaction) is correct and tested.
Task 9: minor (deferred): ConnectionNotFoundError path of disconnectIntegration untested; UnknownProviderError untested for test/disconnect (only connect covers it); connect-integration.ts:147-154 has an unreachable defensive fallback with no covering test.
Task 10: first implementer dispatch (opus) terminated by a provider session rate limit partway through Step 4 (the queue). Partial working tree left uncommitted and intact: run-sync.ts (248), run-sync.test.ts (254), enqueue-sync.ts (39); typecheck fails only on the missing ./drain-sync-queue module and one implicit-any in the test. Ruling P2-C23: re-dispatch on sonnet from the partial working tree rather than discarding it — the same recovery Phase 0/1 used for its Task 5 — cost if wrong: the new implementer inherits a shape it did not choose; it is instructed to change anything that does not hold up.
Task 10: implemented on the re-dispatch (commit 733c789), under review. Carry-forward into Task 11: sync-queue.ts deliberately omits ensureProvidersRegistered() because @/platform/integrations/register-all does not exist until Task 11 — Task 11 must add that call.
Task 10: review 1 — spec compliant, all Produces signatures verified; two-phase boundary structural (SyncFetchContext has no db), sync_jobs enforced, cursor only persisted on success (mutation-tested), claim is a genuine single-statement CAS so concurrent drains cannot double-run. 1 Important, 3 Minor.
Task 10: Important — drainSyncQueue has no try/catch around resumeQueuedSync, but resolve()/prepare() throw synchronously BEFORE execute()'s try/catch (UnknownProviderError, SyncNotSupportedError, ConnectionNotFoundError, ConnectionNotUsableError, SyncDisabledError). Only the deleted-connection case is defended (and it self-heals via FK cascade). A connection that moves to `error` status, or a job disabled between enqueue and drain, throws on every future drain: the whole fetched batch dies with it, the row is never finished so it stays `queued`, and being oldest-first it is picked first forever. The hourly job then reports failed indefinitely and the queue never drains — exactly what the file's own docstring disclaims.
Task 10: Ruling P2-C24: Minor 3 is promoted into the fix round — the engine persists a handler's raw thrown message into sync_runs.error and the audit payload, and Task 18 renders sync run errors in the Settings UI, so an adapter error embedding a token would put a credential on screen and in the audit trail, breaching the absolute "secrets never leave the server" constraint. The engine holds `prepared.credentials` in scope, so it can redact exactly those known values rather than guessing at credential-shaped substrings — cost if wrong: a few lines of redaction that never match anything.
Task 10: minor (deferred): cursor-changed detection uses referential inequality, so a handler mutating the cursor object in place would silently not persist; Resolved.provider is a dead field neither caller consumes.
Task 10: fix round 1 held until Task 11's implementer reports (Task 11 also edits sync-queue.ts; one writer at a time).
Task 10: fix round 1 dispatched -> commit 84114e3 (queue jam guarded, credential values redacted from persisted run errors). Scoped re-review dispatched (base 8502433).
Task 11: implemented (commit 8502433), under review.
Task 12: dispatched (base 84114e3) — Trek adapter; carries P2-11 (onDisconnect never deletes leave data) and the complete removal of trekConfigured/isTrekNotConfigured with every live caller.
Task 11: complete (commits 733c789..8502433, review clean — 0 Critical, 0 Important, 3 Minor). All four carried items verified done: ensureProvidersRegistered wired into sync-queue.ts and wallet-accounts-sync.ts; onDisconnect implements keep/archive/purge with tests asserting resulting Account state and audit payload; the stale sync-provider-accounts comment now matches reality; Phase-1 route/action/guard/toolbar deleted with tests and openapi.json regenerated, no surviving callers.
Task 11: minor (deferred): wallet-accounts-sync.ts:740-746 new doc comment is ambiguous and read literally contradicts the corrected one (could be read as the advisory lock being released before the round trip, which is false); wallet-provider-adapter.ts:110-115 purge spoofs `origin: "manual"` to route around deletionDecision's "synced accounts are never hard-deleted" rule — undocumented hidden coupling, worth a comment or a dedicated parameter; wallet-accounts-sync.test.ts:700-704 asserts ensureProvidersRegistered was called but not that it ran BEFORE the dispatch, which is what its name promises.
Task 10: fix round 1/5 (2 addressed, 0 open — queue-jam guard with a genuine pre-execute() throw in its test, credential redaction funnelled through the single recordFailure point covering both catch sites; commits 8502433..84114e3)
Task 10: complete (commits 5071218..84114e3, review clean)
Task 10: minor (deferred) — FLAG TO THE FINAL WHOLE-BRANCH REVIEW: the new failure write in drain-sync-queue.ts:45-49 is itself not wrapped in try/catch, so if that DB write throws the exception still escapes the loop and kills the rest of the tick — the same failure mode the fix targets, one layer deeper. Also: that catch persists err.message without redaction (safe today because every error reaching it is thrown before prepared.credentials exists, with static messages); redactCredentials has no minimum-length guard so a very short credential value could over-redact (fails safe); the drain failure path writes no audit and does not touch connection status, which is defensible for validation-level failures.
Task 12: implemented (commit f513853), under review. Implementer notes: kept summarize() exported/tested though the brief's claim that the Work page uses it is wrong (it has no caller) — flagged to the reviewer as a possible dead export, since this plan has deleted dead exports twice; also fixed src/app/api/jobs/run/route.ts, a live caller the brief did not name that broke on the narrowed RunTrekSyncJobInput.
Task 13: dispatched (base f513853) — one-time import of the file-mounted Wallet and Trek credentials into the sealed store.
Task 12: review 1 — both named requirements MET with content-asserting tests (onDisconnect structurally cannot delete leave data — no leave repository dependency at all — proven under the harshest policy; trekConfigured/TrekNotConfiguredError/isTrekNotConfigured gone from all ten named call sites, disabledTrekSync genuinely wired into four). Two-phase shape enforced by the type system (SyncApplyContext has no credential field, SyncFetchContext no db). 2 Important, 1 Minor.
Task 12: Ruling P2-C25 (the plan-mandated summarize() finding): DELETE the export. The brief instructed keeping it "because the Work page still formats a pass with it", but that premise is factually false — trek-sync-job.ts:88 builds its detail inline and the only remaining reference is its own test. The plan's authorship does not get to grade its own work, and this phase has already removed dead exports twice on review — cost if wrong: a formatter has to be rewritten if a later phase wants it, from git history.
Task 12: Ruling P2-C26: the Minor test-precision regression is folded into the fix round — trek-sync-job.test.ts dropped an existing assertion on the exact thrown error text ("lock exploded") and now only checks status === "failed". This is precision this task removed, not pre-existing thinness, so it is restored rather than deferred — cost if wrong: one assertion.
Task 12: fix round 1 held until Task 13's implementer reports (one writer at a time).
Task 12: fix round 1 dispatched -> commit cc97b30. Scoped re-review dispatched (base 982efb1).
Task 13: complete (commits f513853..982efb1, review clean — 0 Critical, 0 Important, 2 Minor). All three named requirements verified: no leak path (readTrimmed interpolates only the path and OS error, never content; the single console.log prints a struct that cannot carry a credential; audit payload is {imported, skipped} and recordAudit was confirmed to persist `after` verbatim with no redaction layer, so caller discipline is the whole guarantee and both callers hold it); idempotence tested by running twice and asserting the stored credential still decrypts to the FIRST run's value; degenerate inputs distinguish "not mounted" from a permission error rather than swallowing it. Correctly uses createCredentialCipher (raw key) rather than credentialCipher() which would pull the whole app env.
Task 13: Ruling P2-C27 (resolves the reviewer's ⚠️): import-file-credentials names "wallet" and "trek" literally outside an adapter file. This is a sanctioned exception, not a breach — the task IS a two-provider one-time migration and the brief's FileCredentials interface mandates the names; the constraint exists so a third provider can be added without touching the framework, and a retired one-time migration script is not framework — cost if wrong: the script mentions two products it exists to migrate.
Task 13: minor (deferred): a present trek token file with TREK_URL unset is reported as reason "no_file", which misleads an operator debugging a half-configured mount (mirrors an existing codebase convention); the script lets Node exit naturally while sibling one-off scripts call process.exit(0).
Task 12: fix round 1/5 (3 addressed, 0 open — error text points at reconnect not an env var, summarize() truly deleted with no loosened types, error-content assertion restored; commits 982efb1..cc97b30)
Task 12: complete (commits 84114e3..cc97b30, review clean)
Task 12: note — env.ts still declares TREK_TOKEN_FILE and reads it; that removal belongs to Task 19 (env/compose) and Task 16 (probes.ts/resolve.ts capability check). Verify it is gone by the end of the phase.
Task 14: review 1 — spec compliant, all six routes match the brief's table; CSRF binding verified structurally (the UNSAFE_METHODS/session check is app.use("*") registered before registerAllRoutes, so it covers all four new mutating routes, and an itest proves 403 csrf_required); credential non-leak engineered not just tested (connectionDto has no field able to carry one, plus a JSON.stringify content assertion); Task 11's deleted manual-sync coverage genuinely replaced. 1 Important (plan-mandated), 3 Minor.
Task 14: Ruling P2-C28: the silent-truncation finding is fixed by clamping the accepted ?limit= range to [1,10] rather than by adding a direct repository query. The route validates 1-200 and answers 422 outside it, which advertises a capacity the data source (listIntegrations, hard-capped at 10) does not have, and SyncRunsPageSchema carries no total/hasMore for a client to detect the truncation. Clamping is honest and minimal; a real paginated runs endpoint belongs to Phase 9's Management pages if one is ever wanted — cost if wrong: the Settings runs table can never show more than 10 runs this phase, which is what it shows today anyway.
Task 14: minor (deferred): no test exercises the new ?limit= parsing (default, boundary, or the 422 path) though that logic is implementer-added beyond the brief; three itest cases assert status/shape but not the specific error codes (not_found, permission_denied, ok:true); syncRunsRoute fetches the full listIntegrations result and filters client-side for one provider.
Task 14: fix round 1 held until Task 15's implementer reports (both touch the integrations api module).
Task 14: fix round 1 dispatched -> commit 1a7b05c (limit clamped to [1,10]). Scoped re-review dispatched (base 4a68d5b).
Task 15: implemented (commit 4a68d5b), under review on opus (only unauthenticated endpoint in the phase). Implementer disclosed two deviations from the brief's literal code because it did not compile against installed versions: Hono/zod-openapi body-schema handling, and a MiddlewareHandler<ApiEnv> cast — the cast is on this plan's own defect list, flagged for the reviewer to judge.
Task 16: dispatched (base 1a7b05c) — capability probes move from env checks to connection state; owns the last capability-layer references to the retired Trek env mechanism.
Task 14: fix round 1/5 (1 addressed, 0 open — limit range [1,10] with hard 422 rejection rather than silent coercion, default 10 inside the range, OpenAPI description and regenerated json both updated, six-case test asserting error.code not just status; commits 4a68d5b..1a7b05c)
Task 14: complete (commits cc97b30..1a7b05c, review clean)
Task 15: review 1 (opus) — spec compliant; mandated shape verified as committed (verify -> record -> enqueue queued/webhook -> return; no provider fetch/apply reachable from the handler, so the earlier inline-under-system-context draft is NOT what landed). All five named requirements MET: public prefix bypasses auth, CSRF and the RETURNED rate-limit handler (wrapped, not edited in place — the flagged trap avoided) and reaches only /api/v1/webhooks/, with startsWith matching so encoded/cased variants fail safe into the authenticated path; shared constant-time verifier used by both adapters, no hand-rolled comparison; flat 404 for every rejection reason with nothing echoed; enqueue-not-run proven behaviourally (provider fetch counter 0 after the webhook, 1 only after the drain) and the user-owned write proven to happen later under the owner's context via actorUserId. 2 Important, 6 Minor.
Task 15: Important 1 — unguarded throws in the candidate loop: d.cipher.open throws CredentialCryptoError for a blob whose keyId left APP_ENCRYPTION_KEY, aborting the loop so later candidates are never tried (one user's stale blob denies webhook delivery to every other user of that provider), and because inSystemContext is a real transaction the rollback discards the webhook_deliveries row too. enqueueSync's SyncDisabledError is the same shape, latent today. The webhook handler is the only one in the file with no catch, which is why these surface as 500s to a provider that will retry a 5xx indefinitely.
Task 15: Important 2 — c.req.text() reads an unbounded body before hashing, and every unverified request costs a transaction, a scan of connected connections, N decrypts, N HMACs and an INSERT, on the one endpoint with no rate limiter.
Task 15: Ruling P2-C29: two Minors are promoted into the fix round. (a) The missing route-level test that a SIGNED but non-JSON body returns 404 rather than Hono's 400 — that assertion is what pins deviation 1, and without it a future "use a proper Zod schema here" edit silently restores parse-before-verify, which is the security property this endpoint exists to have. (b) The `as unknown as MiddlewareHandler<ApiEnv>` cast — the reviewer verified it is currently safe, but this plan lists casts-used-to-silence-a-type-error as defects, and making rateLimit generic removes it in one line while restoring the checking that would catch a future variable mismatch — cost if wrong: one test and one signature change.
Task 15: minor (deferred) — CARRY TO PHASE 9 HARDENING: no replay protection (a captured signed delivery can be replayed indefinitely, each queueing another run; payloadHash is already stored and would support cheap dedupe); webhook_deliveries growth needs a retention job alongside the deferred rate limit. Also deferred: provider existence is timing-distinguishable (nothing secret, provider codes are public in openapi.json); two rejection unit tests assert the return value but not the absence of side effects and share one `it` with a third case; only the first event name reaches the delivery row when a delivery maps to several sync requests.
Task 15: fix round 1 held until Task 16's implementer reports (one writer at a time).
Task 16: complete (commits 1a7b05c..f2c780c, review clean — 0 Critical, 0 Important, 3 Minor). Both named requirements met: no TREK_TOKEN_FILE reference remains outside env.ts (which Task 19 retires); empty states read "not connected yet", never a fabricated zero. Reviewer cross-checked the five Settings hrefs and /settings/integrations/wallet against tasks 17 and 18's briefs — no naming drift, the 404s are temporary. Implementer added a positive navigation test (wallet connected => Expenses/Interests appear) the brief's own step omitted; that is the exact assertion the phase exit criterion needs.
Task 16: minor (deferred): capability resolution now costs one extra DB round trip per render (concurrent via Promise.all, brief-mandated); stale prose comment naming trekConfigured() at trek-sync.ts:117 (outside this task's files — fold into Task 19); one over-long line in cards.ts.
Task 15: fix round 1/5 (3 addressed, 1 open; commits f2c780c..2214e8e). ADDRESSED: candidate-loop catches (verified narrow — crypto.open only throws CredentialCryptoError; both catches sit inside the system transaction so catching COMMITS and the delivery row survives, and neither wraps webhook.verify so a genuine success cannot be swallowed); the signed-non-JSON route test (signature genuinely valid over the exact non-JSON body, so it pins the behaviour rather than passing for the wrong reason); the cast removed by making rateLimit generic with a default type parameter, no any/cast reintroduced.
Task 15: OPEN — Important 2 only half closed. routes.ts:402-404 pre-checks Content-Length, which stops an honest caller, but routes.ts:412 still calls the unbounded c.req.text() and the byteLength check at :416 runs only after the body is fully buffered. A caller that omits Content-Length or uses chunked transfer-encoding — the obvious way to defeat a header check, and exactly what an attacker would do — still forces the unbounded read the finding is named after. Confirmed no framework-level cap exists (no next.config limit; App Router route handlers and a bare node server impose none).
Task 15: minor (deferred): the enqueue-loop catch is broader than SyncDisabledError, so a genuine infrastructure failure would be recorded as a rejected delivery and surfaced as a flat 404 rather than a 500 that alerts operators — narrowing with an instanceof check and a re-throw would be strictly safer; a multi-SyncRequest webhook hitting a disabled kind mid-loop does not roll back already-queued runs (unreachable today).
Task 15: fix round 2 held until Task 17's implementer reports (one writer at a time).
Task 17: complete (commits 2214e8e..23589c9, review clean on what it built — 0 Critical, 3 Important all concerning pre-existing links outside its file list, 2 Minor). Access control verified genuinely server-side and defence-in-depth: admin/page.tsx gates on requirePrincipalOrRedirect + permissions.has("admin.users") with notFound() BEFORE any query, and loadUsers() independently asserts the same permission, with a unit test that would fail with a connection error if the check ever moved after the query. member/viewer confirmed not to carry admin.users, so the gate is not vacuous. Security page states honestly that it lists the current session only and why.
Task 17: Ruling P2-C30: the three dangling-link Important findings are assigned to Task 18 rather than a fix round here. They are pre-existing links in files outside this task's scope, and Task 18 is their natural home — it builds /settings/integrations and /settings/integrations/[provider], which is exactly where the Wallet CTAs should point. The reviewer asked for a named follow-up rather than a note in a report that ships nowhere; this is that assignment — cost if wrong: three CTAs land on Personal for one more task.
  - src/app/(app)/page.tsx:274-276 (accounts sync card "Settings") and src/modules/accounts/ui/AccountsToolbar.tsx:39-41 ("Connect/Manage Budget Makers Wallet") -> /settings/integrations/wallet
  - src/app/(app)/work/page.tsx:186 ("Check the job runs") -> /settings/admin (or gate the CTA's visibility on admin.users, since that is where job runs now live)
  - src/app/(app)/finance/funds/page.tsx:46 ("Open settings") -> a destination that actually helps, or drop the CTA
Task 17: minor (deferred): the mobile settings gear is a generic entry point and landing on Personal is fine; loadProfile has no unit test (thin unconditional read, brief did not ask).
Task 15: fix round 2/5 (2 addressed, 0 open — readBodyCapped streams from c.req.raw.body, checks the running total BEFORE pushing each chunk, cancels the reader on overflow and releases it in a finally on every path; chunks stay Uint8Array and are decoded once via Buffer.concat so a multi-byte character split across a boundary cannot corrupt the signature; the new test sends a ReadableStream with no Content-Length and asserts 17 of 32 chunks were pulled, which distinguishes early-abort from drain-then-check, and the implementer verified it fails (33/32) against the old code. Enqueue catch narrowed to SyncDisabledError with a re-throw that reaches app.onError as a 500 rather than an unhandled rejection or a misleading 404; commits 23589c9..b620b1f)
Task 15: complete (commits 285b9de..b620b1f, review clean)
Task 18: implemented (commit 2643e84, DONE_WITH_CONCERNS), under review. Concerns: (1) the brief's manual browser walkthrough with a real Wallet token was not possible in this environment — Task 20 owns end-to-end verification; (2) for carried item 3 it repointed work/page.tsx's "Check the job runs" at a plain /settings/admin link rather than gating on admin.users, because that page still uses the legacy single-user auth path rather than the RBAC Principal — flagged to the reviewer to judge whether that leaves a non-admin a visible dead-end.
Task 19: dispatched (base 2643e84) — docs/env/deploy (the phase's release gate: APP_ENCRYPTION_KEY must reach docker-compose.yml and .env.example or the phase cannot ship) PLUS the batched deferred-minor cleanup wave, in two separate commits.
Task 19: Ruling P2-C31: three deferred findings are explicitly EXCLUDED from the cleanup wave. Folding the version byte into the AES-GCM AAD would invalidate every credential blob already sealed, including the ones the production import created — that is a migration, not a tidy-up. Changing cursor-changed detection from referential inequality to an explicit flag is a behavioural change needing its own design. Webhook replay protection, delivery retention and rate limiting are carried to Phase 9 by recorded decision — cost if wrong: three findings stay open into Phase 9, all documented.
Task 18: complete (commits b620b1f..2643e84, review clean — 0 Critical, 0 Important, 2 Minor). All four carried items verified independently: syncIntegrationAction sends kind only when the form provides one and runSync defaults it internally (with a test on the no-kind path); the runs table renders exactly the 10 it receives with no pagination affordance; the three dangling links repointed (home card and AccountsToolbar -> /settings/integrations/wallet, funds CTA dropped, work page -> /settings/admin); ErrorInline and Toast imported from @/components/ui. Every credential path traced to a dead end. DisconnectForm always shows describeDisconnectPolicy before the destructive action and resets the confirm when the policy changes, so a stale confirm cannot fire against an unseen policy.
Task 18: Ruling P2-C32 (resolves the reviewer's judgment call): the ungated /settings/admin link in work/page.tsx stands. The destination enforces admin.users itself with notFound(), so a non-admin gets a 404 rather than an authorization bypass; the deployment has one user, who is the owner; and gating the CTA would mean wiring RBAC into a page still on the legacy single-user auth path, which belongs to Phase 8 — cost if wrong: a non-owner, once Phase 8 creates one, sees a CTA that 404s.
Task 18: minor (deferred): that CTA's UX once non-owner roles exist; ConnectionActions discriminates two ActionResult payload shapes with `"message" in result.data`, which is not type-narrowed and would silently pick the wrong branch if a third action reused the component with an overlapping field name.
Task 19: review 1 — spec compliant; RELEASE GATE VERIFIED: docker-compose.yml sets APP_ENCRYPTION_KEY=${DASHBOARD_APP_ENCRYPTION_KEY}, .env.example defines it, env.ts reads it — names line up end to end, so the stack boots once an operator populates the value. WALLET_TOKEN_FILE, TREK_URL, TREK_TOKEN_FILE and the ./secrets volume mounts are gone from compose and .env.example with no dangling reference anywhere. All 11 cleanup items verified done; the three behaviour-changing ones (7, 8, 10) have tests the reviewer independently confirmed fail on the pre-fix code — including empirically checking that Node's lenient base64 really does decode a space-embedded key to the same 32 bytes. The item-11 "nine not ten files" discrepancy was independently confirmed accurate. None of the three out-of-scope changes appear. 1 Important, 1 Minor.
Task 19: Important — docs/deploy/phase-2-runbook.md step 3 tells the operator "do not yet remove" the token-file env entries and volume mounts, but the docker-compose.yml committed in the same task has already removed them, and no step says to temporarily restore them for that deploy. Followed literally the import reports skipped:["wallet","trek"] rather than imported. Self-correcting (step 4 checks for exactly this) but the bridging step is missing — and this is the file the task exists to get right.
Task 19: minor (deferred): docs/integrations/README.md documents the key format but not the new round-trip base64 strictness from cleanup item 7.
Task 19: fix round 1 held until Task 20's implementer reports — Task 20 also writes the Phase 2 runbook (the operator walkthrough section), so they would collide.
Task 19: fix round 1 closed by Task 20 (commit pending at ledger time) — `docs/deploy/phase-2-runbook.md` step 3 rewritten to state plainly that the checked-in `docker-compose.yml` has already dropped `WALLET_TOKEN_FILE`/`TREK_URL`/`TREK_TOKEN_FILE` and both `./secrets/*-token` mounts (Task 19 removed them for good, not "later"), and gives the exact four lines an operator must add back to the `dashboard-app` service *for that one deploy only* so step 4's import can read the files. Also flagged: the import script needs `TREK_URL` itself, not just the token file (`scripts/import-file-credentials.ts` only imports Trek when both are present), which the original runbook wording did not make clear. Step 6 now says to revert that temporary edit (equivalent to `git checkout -- docker-compose.yml`) rather than describing a fresh removal. Step 8 (rollback) cross-references the same block. No code changed — docs only.
Task 20: ran the full gate fresh (typecheck, unit, integration, build, e2e, plus the OpenAPI drift test which is inside the unit suite) — all green: typecheck clean; 704 unit tests / 70 files; 48 integration tests / 19 files (`npm run test:db:up` then `npm run test:integration`); `npm run build` succeeds and lists `/finance/expenses`, `/finance/interests`, `/settings/integrations`, `/settings/integrations/[provider]`, `/settings/admin` in the route table; `npm run e2e` (4/4) run against a local `next dev` pointed at the throwaway `dashboard-postgres-test` database (already schema-migrated by the integration suite) — the e2e config has no `webServer` block, so a server must be started by hand per `tests/e2e/README.md`, and this was not attempted against the running production container. Confirmed by grep that no source or script outside `scripts/import-file-credentials.ts` reads `WALLET_TOKEN_FILE`/`TREK_TOKEN_FILE`/`walletToken`/`trekConfig(` (the one hit in `wallet.ts` is a historical doc comment, not a read); confirmed `docker-compose.yml` carries none of the three retired vars.
Task 20: the brief's manual browser walkthrough (real Wallet/Trek tokens, live UI) was not run — no such token or authenticated browser session exists in this environment (also flagged honestly by Task 18's own report). Verified everything reachable without one instead: the five Settings/Finance/Admin routes exist and render in the build; `src/platform/capabilities/navigation.test.ts` and `resolve.test.ts` prove Expenses/Interests appear only when Wallet's connection state is `connected` (not `error`, not `not_configured`) and Administration only with `admin.users`; `src/modules/integrations/api/webhook.itest.ts` proves the signed/unsigned/queued/drained webhook path against a real Postgres. Wrote the missing step as an explicit numbered operator checklist in `docs/deploy/phase-2-runbook.md` §9, using the actual UI copy and button labels (`Test connection`, `Sync now`, `describeDisconnectPolicy`'s three exact sentences) read from source, not paraphrased from the brief.
Task 20: found and recorded as a deployment prerequisite: the production `.env` (dated 2026-09-01, before this phase) does not define `DASHBOARD_APP_ENCRYPTION_KEY` — confirmed by name-only grep, no value read or logged. Phase 2 cannot deploy until the runbook's step 2 is run and that value is added; every commit from Task 4 onward refuses to boot without it (Ruling P2-C18).
Task 20: all 20 tasks complete. Phase 2 gate green at `d04e917`. Branch complete pending the controller's final whole-phase review.
